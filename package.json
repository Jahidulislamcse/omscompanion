{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build && node -e \"import fs from 'fs'; fs.rmSync('build', {recursive: true, force: true}); fs.cpSync('public/build', 'build', {recursive: true});\"",
        "dev": "vite"
    },
    "dependencies": {
        "@inertiajs/react": "^2.0.3",
        "axios": "^1.11.0",
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.3.1",
        "laravel-vite-plugin": "^2.0.0",
        "vite": "^7.0.7"
    }
}
